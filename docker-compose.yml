version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:8000" # Maps host port 80 to container port 8000
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    volumes:
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000